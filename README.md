# skilly-pgp
Skilly implements the Parallel Guardian Pattern (PGP), a parallel oversight layer that uses deterministic embeddings to detect tool intent while the LLM responds. A clean, modular approach for improving reliability, grounding and action-accuracy in LLM systems.
